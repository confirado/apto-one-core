<!DOCTYPE html>
<html>
<head>
    <style>
        html {
            font-family: sans-serif;
            -ms-text-size-adjust: 100%;
            -webkit-text-size-adjust: 100%;
            font-size: 13px;
            -webkit-tap-highlight-color: rgba(0, 0, 0, 0);
        }

        body {
            margin: 1em;
            font-family: "Helvetica Neue", Helvetica, Arial, sans-serif;
            font-size: 13px;
            color: #333333;
            background-color: #FFFFFF;
        }

        b, strong {
            font-weight: bold;
        }

        small {
            font-size: 80%;
        }

        sub, sup {
            font-size: 75%;
            line-height: 0;
            position: relative;
            vertical-align: baseline;
        }

        sup {
            top: -0.5em;
        }

        sub {
            bottom: -0.25em;
        }

        a {
            background-color: transparent;
            color: #337ab7;
            text-decoration: none;
        }

        th {
            text-align: left;
            background: #F0F0F0;
        }

        table {
            background-color: transparent;
            border-collapse: collapse;
            border-spacing: 0;
            width: 100%;
            max-width: 100%;
            margin-bottom: 20px;
            margin-left: 0;
            margin-right: 0;
        }

        table th {
            padding: 8px;
            margin: 0;
            line-height: 1em;
            vertical-align: middle;
            border-top: 1px solid #ddd;
            border-bottom: 1px solid #ddd;
        }

        table td {
            padding: 8px;
            vertical-align: top;
        }
    </style>
</head>

<body>
<p>
    {{ contentSnippets.customer.newRequest.prefix[locale]|default('') }} {{ customer.name }} {{ customer.surname }} {{ contentSnippets.customer.newRequest.suffix[locale]|default('') }}
</p>

{% if pdfToCustomer == false %}
    <p>
        <strong style="color: red;">{{ contentSnippets.customer.contractNotReceived[locale]|default('') }}</strong>
    </p>
{% endif %}

<p>
    <strong>{{ contentSnippets.customer.orderNumber[locale]|default('') }}</strong> {{ randomNumber }}
</p>

{% if showPrices %}
<p>
    <strong>{{ contentSnippets.customer.totalAmount[locale]|default('') }}</strong> {{ quantity > 1 ? quantity ~ ' * ' : '' }}{{ prices.sum.price.formatted }}
</p>
{% endif %}

<p>
    <strong>{{ contentSnippets.customer.linkForConfiguration[locale]|default('') }}</strong>
    <a href="{{ url('apto_base_infrastructure_aptobase_frontend_index') }}#/configuration/basket/{{ configurationId }}">
        {{ contentSnippets.customer.configurationLink[locale]|default('') }}
    </a>
</p>
<p>
    <strong>{{ contentSnippets.customer.details[locale]|default('') }}</strong>
</p>
<table>
    <thead>
    <tr>
        <th>{{ contentSnippets.customer.configurationField[locale]|default('') }}</th>
        <th>{{ contentSnippets.customer.configurationValue[locale]|default('') }}</th>
    </tr>
    </thead>
    <tbody>
    {% for key in customer|keys %}
        <tr>
            <td>{{ key }}</td>
            <td>{{ customer[key] }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<p>
    <strong>{{ contentSnippets.customer.configuration[locale]|default('') }}</strong> {{ product.name[locale]|default('') }}
</p>

<table>
    <thead>
    <tr>
        <th>{{ contentSnippets.customer.configurationField[locale]|default('') }}</th>
        <th>{{ contentSnippets.customer.configurationValue[locale]|default('') }}</th>
    </tr>
    </thead>
    <tbody>
    {% for key in state|keys %}
        <tr>
            <td><strong>{{ key }}</strong></td>
            <td>
                {% for element in state[key] %}
                    {{ element.name }}

                    {% if element.values|length > 0 %}
                        <br /><br />{{ contentSnippets.customer.configurationProperties[locale]|default('') }}

                        {% for valueKey in element.values|keys %}
                            <br />{{ valueKey }}: {{ element.values[valueKey] }}<br />
                        {% endfor %}
                    {% endif %}
                    {% if loop.last == false %}
                        <br>
                    {% endif %}
                {% endfor %}
            </td>
        </tr>
    {% endfor %}
    <tr>
        <td><strong>{{ contentSnippets.customer.configurationQuantity[locale]|default('') }}</strong></td>
        <td>{{ quantity }}</td>
    </tr>
    </tbody>
</table>

</body>
</html>
